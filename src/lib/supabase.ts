// Deprecated: use "@/lib/supabase/client" (browser) or "@/lib/supabase/server"
// (server components / route handlers). Re-exported here so any older imports
// keep working with the cookie-aware client.
export { createClient } from "./supabase/client";
